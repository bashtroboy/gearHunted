# PostgreSQL configuration for Google Cloud SQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gearhunted
DB_USER=postgres
DB_PASSWORD=your_password

# For Google Cloud SQL (production)
# DB_HOST=your-cloud-sql-ip
# DB_USER=postgres
# DB_PASSWORD=your_secure_password
# DB_NAME=gearhunted

# Google Cloud SQL Connection (when using Cloud SQL Proxy)
# INSTANCE_CONNECTION_NAME=your-project:region:instance-name